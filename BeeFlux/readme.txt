Create the Conda Environment in this directory (after installing) by:
	conda env create -f environment.yml

Then activate it in command prompt:

	activate beeflux
	
Then start spyder:
	
	spyder
	
Navigate to and run cv_test!